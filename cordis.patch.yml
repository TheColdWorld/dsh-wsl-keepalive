# wsl-keepalive bundle patch: insert this plugin row (both the Host and Client halves
# are mounted by this single row; the Client half is discovered via package.json's
# dsh.client declaration).
# Takes effect after installation via `dsh plugin --profile web add <this package path>`.
- insert:
    - id: wsl-keepalive
      name: 'wsl-keepalive'
